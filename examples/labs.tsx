import React from "react";
import ReactDOM from "react-dom";
import Ttaio from "../labs";

ReactDOM.render(
  <React.StrictMode>
    <Ttaio />
  </React.StrictMode>,
  document.getElementById("root")
);
